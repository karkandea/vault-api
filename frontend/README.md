# Vault Frontend

React + Vite frontend for Vault Product Management API.

## Setup

```bash
npm install
npm run dev
```

Requires backend running at `http://localhost:5278`.
Set `VITE_API_URL` in `.env.local` to override API URL.